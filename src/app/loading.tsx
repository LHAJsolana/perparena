import { PageHeader } from "@/components/ui/page-header";

export default function Loading() {
  return (
    <PageHeader
      eyebrow="Loading"
      title="Preparing PerpArena"
      description="The simulated competition workspace is loading."
    />
  );
}
